name: CI
on:
  workflow_call:
    inputs:
      appName:
        required: true
        type: string
      apptype:
        required: true
        type: string

jobs:
  build:
    runs-on: self-hosted
    # Only run build if it's a maven app
    if: ${{ inputs.apptype == 'maven' }}
    steps:
      - uses: actions/checkout@v4

      - name: Build with Maven
        if: ${{ inputs.apptype == 'maven' }}
        run: mvn clean package
      - name: upload artifact
        if: ${{ inputs.apptype == 'maven' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.appName }}-jar
          path: target/${{ inputs.appName }}-1.0.jar
  test:

    runs-on: self-hosted
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      # Runs a set of commands using the runners shell
      - name: Run ${{ inputs.apptype }} test
        run: echo ${{ inputs.apptype }} test
  docker:
    needs: test
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
#      - name: Configure AWS Credentials via Keys
#        uses: aws-actions/configure-aws-credentials@v4
#        with:
#          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          aws-region: us-east-1
      - name: Run Docker Login with Secrets
        env:
            AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
            AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: aws ecr-public get-login-password --region us-east-1 | docker login --username AWS --password-stdin public.ecr.aws/u4j6q5s8
      - uses: actions/download-artifact@v4
        if: ${{ inputs.appName == 'maven' }}
        with:
          name: shipping-jar
          path:  ./
      - name: build docker image
        run: docker build -t public.ecr.aws/u4j6q5s8/${{ inputs.appName }}:v1 .
      - name: push to aws ecr
        run: docker push public.ecr.aws/u4j6q5s8/${{ inputs.appName }}:v1
  dev:
   needs: docker
   runs-on: self-hosted
   steps:
     - uses: actions/checkout@v4
       name: checkout
    
        

     
       with:
        repository: pdevops87/roboshop-helm-v3-final
        role-to-assume: arn:aws:iam::041445559784:role/dev-eks-role
        aws-region: us-east-1
     - uses: aws-actions/configure-aws-credentials@v4
       name: aws credentials
       with:
        role-to-assume: arn:aws:iam::041445559784:role/github-eks-role
        aws-region: us-east-1
    
   
     - name: run helm application
       env:
         AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
         AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
         AWS_DEFAULT_REGION: us-east-1
       run: |
         export KUBERNETES_MASTER=$(kubectl config view -o jsonpath='{.clusters[0].cluster.server}')
         aws eks update-kubeconfig --name dev-cluster --region us-east-1
         helm upgrade -i ${{ inputs.appName }} . -f pass-to-templates/${{ inputs.appName }}.yaml

#     - name: Wait for LoadBalancer DNS
#       run: |
#
#          LB_HOST=""
#          while [ -z "$LB_HOST" ]; do
#            LB_HOST=$(kubectl get svc argocd-server -o jsonpath="{.status.loadBalancer.ingress[*].hostname}" >>/dev/null)
#            if [ -z "$LB_HOST" ]; then
#              echo "Waiting for AWS to allocate hostname..."
#              sleep 10
#            fi
#          done
#
#          LB_HOST=$(kubectl get svc argocd-server -o jsonpath="{.status.loadBalancer.ingress[0].hostname}")
#          echo "LB_HOST=$LB_HOST" >> $GITHUB_ENV # Save variables in Github env
#          echo "$LB_HOST is ready!"
#
#     - name: ArgoCD Login
#       run: |
#         # Fetch password and login using the saved LB_HOST
#         ARGOCD_PASS=$(kubectl get secrets argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 --decode)
#         echo "ARGOCD_PASS" >> $GITHUB_ENV
#         argocd login ${{ env.LB_HOST }} --insecure --username admin --password ${{ env.ARGOCD_PASS }}
#
#     - name: Create and Sync ArgoCD App
#       run: |
#         argocd app create ${{ inputs.appName }} \
#           --repo https://github.com/pdevops87/roboshop-helm-v3-final.git \
#           --path . \
#           --dest-namespace default \
#           --dest-server https://kubernetes.default.svc \
#           --values pass-to-templates/${{ inputs.appName }}.yaml \
#           --upsert
#         argocd app sync ${{ inputs.appName }}
##         argocd app health checks
#         argocd app wait ${{ inputs.appName }} --timeout 60







